FROM python:3.13-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends git ca-certificates && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY server.py /app/server.py
COPY patch_engine.py /app/patch_engine.py

EXPOSE 8877

CMD ["python", "/app/server.py"]